.DS_Store
__pycache__/
*.pyc
.superpowers/
.env
docker-compose.override.yml

# Raw external data sources (large clones/snapshots, refetchable — not checked in)
data/raw

# Git worktrees for isolated feature work
.worktrees

# Local Claude Code settings (machine-specific, not shareable)
.claude/settings.local.json

# impeccable skill scaffolding + generated context (kept locally, not part
# of this project -- add these back individually if you start using it here)
.claude/skills/impeccable/
.github/agents/impeccable-*.md
.github/skills/impeccable/
.github/hooks/impeccable.json
PRODUCT.md
